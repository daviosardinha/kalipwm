#! /bin/sh

wmname LG3D &
vmware-user-suid-wrapper &

# Keep Flameshot running in the session so GUI captures and clipboard actions
# behave consistently in the minimal BSPWM environment.
pgrep -x flameshot > /dev/null || flameshot > /dev/null 2>&1 &

pgrep -x sxhkd > /dev/null || sxhkd &

bspc monitor -d I II III IV V VI VII VIII IX X

bspc config border_width         2
bspc config window_gap          6

bspc config split_ratio          0.52
bspc config borderless_monocle   true
bspc config gapless_monocle      true

bspc rule -a Gimp desktop='^8' state=floating follow=on
bspc rule -a Chromium desktop='^2'
bspc rule -a mplayer2 state=floating
bspc rule -a Kupfer.py focus=on
bspc rule -a Screenkey manage=off

# WALLPAPER - Obsidian v2
# No argument means: restore the wallpaper choice saved by the installer/user.
~/.config/bspwm/scripts/set-obsidian-wallpaper.sh

# POLYBAR - Obsidian v2
~/.config/polybar/obsidian-v2/launch.sh

# CUSTOM
bspc config focus_follows_pointer true

# Full HD resolution
xrandr --output Virtual1 --mode 1920x1080

# PICOM
picom &
bspc config border_width 0

xsetroot -cursor_name left_ptr &
